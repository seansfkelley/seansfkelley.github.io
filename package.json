{
  "name": "seansfkelley.github.io",
  "version": "0.0.0",
  "scripts": {
    "transpile": "tsc && generate-grammar",
    "check-transpile": "npm run transpile && git diff --exit-code",
    "generate-grammar": "peggy -o assets/blazon/grammar.js --format globals --export-var parser assets/blazon/grammar.peg"
  },
  "dependencies": {
    "peggy": "^3.0.2",
    "typescript": "^5.1.6"
  }
}
