{
  "name": "seansfkelley.github.io",
  "version": "0.0.0",
  "scripts": {
    "transpile": "tsc && generate-grammar",
    "check-transpile": "npm run transpile && git diff --exit-code",
    "generate-grammar": "peggy -o assets/blazonry/grammar.js --format globals --export-var parser assets/blazonry/grammar.peg && cp assets/blazonry/grammar.peg assets/blazonry/grammar.txt"
  },
  "dependencies": {
    "peggy": "^3.0.2",
    "typescript": "^5.1.6"
  }
}
